extends Node
## ShieldPool. Unused (shields removed). Kept as stub so scene node doesn't crash.

func add_energy(_amount: int) -> void:
	pass

func get_current_shield_points() -> int:
	return 0

func get_current_energy() -> int:
	return 0

func consume_shield_points(_amount: int) -> void:
	pass
